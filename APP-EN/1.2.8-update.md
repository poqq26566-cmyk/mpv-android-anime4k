**Player Core**

- GPU-NEXT toggle
- Seekbar thumbnail preview (shows video frame when dragging)
- In-player Dolby Vision detection (replaces filename guessing)

**Speed System Redesign**

- Right-side drawer panel replaces popup, with preset management and precise input
- Long-press speed changed to inline slider
- Long-press + swipe left/right for dynamic speed adjustment (8 levels, 0.25x–4x)

**Audio Equalizer**

- 5-band equalizer + bass boost + virtualizer
- Multiple UI optimizations (fixed title bar, scrollable layout)

**Subtitle System**

- Custom font directory, supports importing .ttf/.otf
- Style override toggle relocated, global fallback mechanism (fixes style loss on episode switch)
- Fixed border size precision, external subtitles not showing, ASS subtitle freezing

**Danmaku System**

- Multi-server management (add/delete/enable/disable)
- Search traverses all enabled servers, merges and deduplicates results

**Controls & UI**

- Seek forward/backward supports custom duration (1–300s)
- Double-tap seek duration supports custom input
- Marquee scrolling for long titles (folders/video list/WebDAV)
- Settings page icon improvements

**Bug Fixes**

- Screen orientation stuck after exiting landscape playback
- Duration showing as 0
