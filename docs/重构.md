先看看主文件里所有 `@Deprecated` 字段的情况。 

Searched for text `@Deprecated` (`**/VideoPlayerActivity.kt`), 20 results

这是个大工程，需要先确认这些字段在整个代码库中还有多少直接引用。 

Searched for regex `\.isPlaying|\.currentSpeed|\.savedPosition|\.hasRestoredPosition|\.hasShownPrompt|\.lastPlaybackPosition|\.currentPosition\b|\.duration\b|\.speedBeforeLongPress|\.isHardwareDecoding|\.gestureStartPosition|\.currentVideoAspect|\.lastPositionForBuffering|\.lastPositionUpdateTime|\.isStalledBuffering|\.previousIsPlaying|\.isThumbnailInitialized|\.seekTimeSeconds|\.currentSeries\b|\.currentVideoIndex|\.currentVideoList|\.anime4KEnabled\b|\.anime4KMode\b|\.anime4KQuality|\.hasAutoLoadedSubtitle|\.currentFolderPath|\.isOnlineVideo\b|\.isFromHomeContinue|videoUri\b` (`**/VideoPlayerActivity*.kt`), 100 results

先检查 ViewModel 有哪些 setter，以及各字段的实际使用频率。  

Searched for files matching `**/PlayerViewModel.kt`, 1 match

Searched for regex `fun set` (`**/PlayerViewModel.kt`), 20 results

先统计各字段的实际使用次数，判断清理工作量。  

Ran terminal command: 
$dir = "C:\Users\root\Desktop\FAM4K007\app\src\main\java\com\fam4k007\videoplayer"
$fields = @(
    "videoUri",       "isPlaying",    "currentPosition\b", "duration\b",
    "seekTimeSeconds","isOnlineVideo","currentFolderPath", "isFromHomeContinue",
    "savedPosition\b","hasRestoredPosition","hasShownPrompt","lastPlaybackPosition\b",
    "isThumbnailInitialized","hasAutoLoadedSubtitle",
    "anime4KEnabled\b","anime4KMode\b","anime4KQuality",
    "isStalledBuffering","lastPositionForBuffering","lastPositionUpdateTime",
    "currentSpeed\b","speedBeforeLongPress","isHardwareDecoding\b",
    "gestureStartPosition","currentVideoAspect","previousIsPlaying",
    "currentSeries\b","currentVideoIndex","currentVideoList"
)
$results = @()
foreach ($f in $fields) {
    $count = (Get-ChildItem "$dir\VideoPlayerActivity*.kt" | Select-String -Pattern $f -AllMatches | Measure-Object -Property Matches -Sum).Sum
    $results += [PSCustomObject]@{ Field=$f -replace '\\b',''; Count=$count }
}
$results | Sort-Object Count -Descending | Format-Table -AutoSize


Ran terminal command: cd "C:\Users\root\Desktop\FAM4K007\app\src\main\java\com\fam4k007\videoplayer"; @("videoUri","isPlaying","currentPosition","seekTimeSeconds","isOnlineVideo","currentFolderPath","isFromHomeContinue","savedPosition","hasAutoLoadedSubtitle","isStalledBuffering","anime4KEnabled","anime4KMode","currentSpeed") | ForEach-Object { $c = (Get-ChildItem "VideoPlayerActivity*.kt" | Select-String -Pattern $_ | Measure-Object).Count; "$($_.PadRight(24)) $c" }

现在可以做，但工作量比描述的大很多。关键数据：

| 字段 | 引用次数 | 难度 |
|------|---------|------|
| `videoUri` | 53 | 极复杂 |
| `isPlaying` / `isOnlineVideo` | 22 各 | 复杂 |
| `anime4KMode` | 19 | 中等 |
| `currentPosition` | 18 | 复杂（callback 内直接写入）|
| `seekTimeSeconds` | 12 | 中等 |
| `isStalledBuffering` + 缓冲相关 | ~9 各 | 复杂（每帧更新）|
| `currentSpeed` / `isFromHomeContinue` 等 | 4-7 | 简单 |

**目前的实际情况：**

- ✅ ViewModel 已经有所有对应的 State/Setter
- ❌ 大量扩展函数仍然**直接读 Activity 字段**，而不是 `viewModel.xxx.value`
- ❌ `lastPositionForBuffering`、`lastPositionUpdateTime`、`isStalledBuffering` 在每帧进度回调里**直接写入**，若改成 StateFlow 会有性能问题

**建议分批做：**

| 批次 | 内容 | 字段 |
|------|------|------|
| **批次 1**（可以现在做）| 简单字段，引用少 | `currentSpeed`、`speedBeforeLongPress`、`gestureStartPosition`、`previousIsPlaying`、`isFromHomeContinue` |
| **批次 2**（可以接着做）| 中等字段 | `seekTimeSeconds`、`currentFolderPath`、`hasAutoLoadedSubtitle`、`hasRestoredPosition`、`hasShownPrompt`、`isThumbnailInitialized`、`anime4KEnabled`、`anime4KQuality` |
| **批次 3**（需谨慎）| 高频/高风险字段 | `videoUri`、`isPlaying`、`isOnlineVideo`、`currentPosition`、`duration`、`anime4KMode`、缓冲相关变量 |

要现在开始的话，从**批次 1** 开始？还是直接全做（工作量约 1-2 小时）？