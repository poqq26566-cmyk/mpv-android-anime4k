<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" 
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" 
        android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- 查询能处理 bilibili:// scheme 的应用（哔哩哔哩 App） -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="bilibili" />
        </intent>
    </queries>

    <application
        android:name=".AppApplication"
        android:allowBackup="true"
        android:appCategory="video"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.VideoPlayer">
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 系统 MediaScanner 扫描完成广播接收器 -->
        <receiver
            android:name=".utils.media.MediaScanReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_SCANNER_FINISHED" />
            </intent-filter>
        </receiver>
        <activity 
            android:name=".UserAgreementActivity"
            android:exported="false"
            android:theme="@style/Theme.VideoPlayer" />
        <activity android:name=".SettingsComposeActivity" />
        <activity android:name=".DownloadActivity" />
        <activity android:name=".PlaybackSettingsComposeActivity" />
        <activity android:name=".PlaybackHistoryComposeActivity" />
        <activity android:name=".BiliBiliDanmakuComposeActivity" />
        <activity android:name=".SubtitleSearchActivity" />
        <activity android:name=".AboutComposeActivity" />
        <activity android:name=".LicenseActivity" />
        <activity 
            android:name=".BiliBiliPlayActivity"
            android:exported="false"
            android:theme="@style/Theme.VideoPlayer" />
        <activity 
            android:name=".VideoBrowserComposeActivity"
            android:exported="false" />
        <activity 
            android:name=".VideoListComposeActivity"
            android:exported="false" />
        <activity 
            android:name=".webdav.WebDavComposeActivity"
            android:exported="false" />
        <activity 
            android:name=".webdav.WebDavBrowserComposeActivity"
            android:exported="false" />
        <activity 
            android:name=".tv.TVBrowserActivity"
            android:exported="false"
            android:theme="@style/Theme.VideoPlayer" />
        <activity 
            android:name=".mediainfo.MediaInfoActivity"
            android:exported="false"
            android:theme="@style/Theme.VideoPlayer" />
        <activity 
            android:name=".VideoPlayerActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:launchMode="singleTop"
            android:supportsPictureInPicture="true"
            android:exported="true">
            <!-- 支持从文件管理器等外部应用打开视频文件 -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="video/*" />
                <data android:mimeType="audio/*" />
                <data android:mimeType="*/rmvb" />
                <data android:mimeType="*/avi" />
                <data android:mimeType="*/mkv" />
                <data android:mimeType="application/mp4" />
                <data android:mimeType="application/x-matroska" />
            </intent-filter>
            
            <!-- 支持从浏览器等打开网络视频链接 -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="http" />
                <data android:scheme="https" />
                <data android:mimeType="video/*" />
                <data android:mimeType="audio/*" />
            </intent-filter>
            
            <!-- 支持分享功能 -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="video/*" />
                <data android:mimeType="audio/*" />
            </intent-filter>
        </activity>
    </application>

</manifest>
